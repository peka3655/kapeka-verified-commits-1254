name: apptoken-commit
on: workflow_dispatch
permissions:
  contents: write
jobs:
  c:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: create commit with installation token
        env:
          TOK: ${{ github.token }}
        run: python3 .github/rig_apptoken.py
